{% trans_default_domain 'copywriting' %}

<div class="modal inmodal" id="declineModal" tabindex="-1" role="dialog" aria-hidden="true" style="display: none;">
{{ form_start(decline_form) }}
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">{{ 'decline'|trans }}</h4>
                <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">×</span><span class="sr-only"></span></button>
            </div>
            <div class="modal-body">
                <p>
                    {{ 'decline_article'|trans }}
                </p>
                {{ form_widget(decline_form.comment) }}
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-white" data-dismiss="modal">{{ 'cancel'|trans }}</button>
                <button type="submit" data-action="decline" type="button" class="btn btn-primary">{{ 'decline'|trans }}</button>
            </div>
        </div>
    </div>
{{ form_end(decline_form) }}
</div>
