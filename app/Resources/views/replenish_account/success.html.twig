{% extends 'layout.html.twig' %}

{% trans_default_domain 'replenish_account' %}

{% block title %}
    {% if response.status == constant('CoreBundle\\Services\\ReplenishAccountService::REPLENISH_STATUS_SUCCESS') %}
        {{ 'title_success'|trans }}
    {% else %}
        {{ 'title_fail'|trans }}
    {% endif %}
{% endblock %}

{% block breadcrumb %}
    {{ include('breadcrumb/my_account.html.twig', {'childrens': ['my_account.replenish_account'|trans({}, 'menu'), block('title')]}) }}
{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-lg-6">
            <h1>{{ 'successfully_replenish' | trans }} {{ response.amount }}&nbsp;€</h1>
        </div>
    </div>
{% endblock %}
